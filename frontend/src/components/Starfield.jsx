// Light theme: white stars on a light background are invisible noise, so the
// starfield is disabled. Kept as a component so re-enabling a dark theme (or
// a per-route ambient effect) is a one-line change.
export default function Starfield() {
  return null
}
